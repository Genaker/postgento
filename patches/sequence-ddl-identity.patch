--- a/lib/internal/Magento/Framework/DB/Ddl/Sequence.php
+++ b/lib/internal/Magento/Framework/DB/Ddl/Sequence.php
@@ -25,12 +25,35 @@
         $columnType = Table::TYPE_INTEGER,
         $unsigned = true
     ) {
-        $format = "CREATE TABLE %s (
+        if ($this->isPostgres()) {
+            $format = "CREATE TABLE %s (
+                     sequence_value %s NOT NULL GENERATED BY DEFAULT AS IDENTITY (START WITH %d),
+                     PRIMARY KEY (sequence_value)
+            )";
+            return sprintf($format, $name, $columnType, $startNumber);
+        }
+        $format = "CREATE TABLE %s (
                      sequence_value %s %s NOT NULL AUTO_INCREMENT,
                      PRIMARY KEY (sequence_value)
             ) AUTO_INCREMENT = %d ENGINE = INNODB";
 
         return sprintf($format, $name, $columnType, $unsigned ? 'UNSIGNED' : '', $startNumber);
     }
 
+    private function isPostgres(): bool
+    {
+        try {
+            return \Magento\Framework\App\ObjectManager::getInstance()
+                ->get(\Morozov\PgCompat\DB\EngineResolver::class)
+                ->isPostgres();
+        } catch (\Throwable $e) {
+            return false;
+        }
+    }
+
     /**
      * Return SQL for drop sequence
      *
